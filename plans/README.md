# Implementation Plans

Generated by the improve skill on 2026-07-09, from a four-lane audit
(correctness+security, performance+architecture, tests+DX+docs,
direction+dependencies) of the repo at commit `223d130`, with every finding
re-verified against the cited code before planning. Execute in the order
below unless dependencies say otherwise. Each executor: read the plan fully
before starting, honor its STOP conditions, and update your row when done.

**Selection note**: the fix-plan selection dialog failed to render for the
operator twice, so the advisor applied its recommended default (top 5 fix
findings by leverage) — the operator explicitly selected all four direction
spikes. To add plans for the unplanned findings below, say so.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Secure-input boundary: stop capture/cloud/history in secure fields | P1 | M | — | DONE |
| 002  | Fixture-driven replay harness for the real xAI ingestion path | P1 | M | — | TODO |
| 003  | CLAUDE.md for agent sessions | P1 | S | — | DONE |
| 004  | Small-fixes bundle: tornDown race (xAI + ElevenLabs), /tmp orphan, log redaction, stray comment | P2 | S | — | DONE |
| 005  | Docs & deps batch: README setup, WhisperKit manifest, UNUserNotificationCenter, parakeet pin | P2 | S | — | TODO |
| 006  | Spike: WPM + latency insights (duration into HistoryEntry) | P3 | M | — | PARTIAL (Insights view shipped & wired; duration field + WPM did not) |
| 007  | Spike: retry-from-history with an engine picker | P3 | S-M | — | PARTIAL (Retry shipped for failed entries only; no engine picker) |
| 008  | Spike: per-app Snippets/Dictionary/auto-run-Transforms (ship one store) | P3 | M | — | TODO |
| 009  | Spike: export/import settings bundle | P3 | M | — | TODO |
| 010  | Cloud batch WAV transcode — batch uploads have never worked (empirically proven 2026-07-09) | P1 | M | — | TODO |
| 011  | Fix AssemblyAI deprecated `speech_model` param (hard 400 on every request) | P1 | S | — | TODO |
| 012  | Add ElevenLabs Scribe v2 Realtime engine (additive; xAI untouched & still default) | P2 | M | — | DONE |
| 013  | Retire Groq/Deepgram/AssemblyAI/OpenAI as selectable ASR engines (scaffolding kept) | P2 | S | — | DONE |
| 014  | Scribe RT native polish: `no_verbatim` + `keyterms` from the learned dictionary | P2 | M | — | DONE |
| 015  | Sparkle auto-update (real appcast + EdDSA + notarized publish) | P1 | M | make-dmg / notarize | DONE (feed live & EdDSA-signed, verified 2026-07-27; in-app update UI smoke test still unverified) |
| 016  | Stop capture/cloud streaming when secure input turns on *during* a hold | P1 | M | — | DONE (code+tests; manual smoke pending) |
| 017  | Close the streaming-continuation data race; bound the Parakeet sidecar | P1 | M | — | DONE |
| 018  | Get disk I/O off the main thread on the dictation hot path | P2 | M | — | DONE (code+tests; manual smoke pending) |
| 019  | Make the docs tell the truth (README engines/paths, release flow, index) | P2 | S | — | DONE |
| 020  | Polish bundle: regex precompile, log redaction, log path, signing-key default | P2 | S | after 017 | DONE |

Status values: TODO | IN PROGRESS | DONE | PARTIAL (with what's missing) | BLOCKED (with one-line reason) | REJECTED (with one-line rationale) | SCOPED

## Dependency notes

- 002 should land before any extraction of the stitch logic out of
  `XAIStreamingTranscriber` (audit finding #9, unplanned) — fixtures are the
  net that makes that refactor safe.
- 004's Step 1 (tornDown lock) relies on the existing stitch suites as its
  regression net; running it after 002 gives it the stronger net, but it is
  not blocked on it.
- 007 and 006 both touch the `transcribeAndLog` chain — coordinate if run
  concurrently (prefer sequential).
- Response-handling dedupe (audit finding #8, unplanned) should precede any
  new provider work, including the noted Groq-cleanup idea.
- **011 is now DEFERRED** (plan 013, 2026-07-09): AssemblyAI is no longer a
  selectable engine in the app (scaffolding only), so the broken
  `speech_model` param it would fix is unreachable from the UI. Not deleted —
  revisit only if AssemblyAI is ever re-added to `CloudModel.selectable`.

## STT bake-off results (2026-07-09, 100 scored attempts on 5 real dictations, WAV)

| Config | Failure rate | Deterministic | Median latency | Mean WER |
|---|---|---|---|---|
| elevenlabs/scribe_v2 | 0% | yes | 842ms | **0.024** |
| assemblyai/universal-3-5-pro | 0% | yes | 3,520ms | 0.024 |
| xai/grok-stt | 0% | yes | **507ms** | 0.050 |
| deepgram/nova-3 | 0% | yes | 667ms | 0.100 |

Could not be scored: groq (dead key — operator must rotate); deepgram/nova-4
(403, account lacks model access); both providers' native-CAF paths (broken —
see plans 010/011). Harness + raw data: `scratchpad/bakeoff/` (reusable).

## Operator action items (not executor plans)

- ~~CI is dead at the account level~~ **RESOLVED 2026-07-10 — misdiagnosis.**
  The operator intentionally turned GitHub Actions off (2026-07-07); the
  zero-step failures were pushes hitting a deliberately unfunded/disabled
  service, not an outage. The workflow file was removed on 2026-07-10 so
  pushes stop generating failed-run noise. This repo has no CI by design;
  local build+test (see CLAUDE.md) is the verification. Do not re-add.

## Re-audit 2026-07-27 (commit `5560a7b`)

Second full pass — five lanes (correctness+perf, security, tests+debt+UI,
direction+docs+DX, and an external-dependency freshness lane). Every finding
below was re-verified against the cited code by the advisor before planning.
Baseline at audit time: **113 tests, 0 failures**, build green.

Plans 016–020 came out of it. Selection was the advisor's default (top 5 by
leverage) — the session was non-interactive, so the operator did not choose.

### External dependency state (verified 2026-07-27)

- **Sparkle** — `Package.resolved` pins **2.9.4**; CVE-2026-47121 (delta-update
  symlink traversal) and CVE-2026-47122 (AppInstaller XPC) were fixed in
  2.9.2, so this app is already past both. **No action.** Note `project.yml`
  says `from: "2.6.0"` — the floor, not what resolves.
- **WhisperKit** — resolves to **0.18.0**; the project renamed itself to
  `argmax-oss-swift` at v1.0.0 (May 2026), a declared breaking release
  (removed deprecated APIs, async `TokenSampling.update`,
  `supressTokens`→`suppressTokens`, Swift 6 concurrency). The `from: "0.9.0"`
  pin caps below 1.0.0, so nothing auto-upgrades. A future major bump means
  changing the SPM URL. **No action now.**
- **ElevenLabs Scribe v2 Realtime** — on 2026-07-20 ElevenLabs added
  `scribe_v2_realtime_turbo` and `scribe_v2_realtime_lite` model IDs plus a
  `secondary_languages` param. `no_verbatim` and `keyterms` are unchanged and
  show no deprecation signal. **Optional upside**, not a fix — worth a
  latency/accuracy bake-off against the current `scribe_v2_realtime` using the
  harness from the 2026-07-09 round.
- **xAI Grok STT**, **parakeet-mlx** (0.5.2), **macOS 26.x** (26.5.2 GA; 26.6
  still RC) — no material change.
- **Update channel is live**: the public appcast returns 200 and lists
  through **0.1.4** (double-paste unicode inject), with items carrying
  `sparkle:edSignature`. (Re-audit prose originally cited 0.1.1/0.1.2; feed
  has advanced — treat **0.1.4** as current at execute time.)

### Audited but not planned this round

- **`VoiceCommands.swift` is dead code** — defines two working voice commands
  ("scratch that", "delete last sentence") and is referenced from **nowhere**
  (exhaustive grep across `Sources/` and `Tests/` finds only its own
  definition). It is the one correction/expansion mechanism with no store, no
  UI, and no pipeline wiring, while Dictionary, Snippets, and Transforms all
  have full CRUD. Either wire it up or delete it — leaving a written feature
  inert is the worst of both. S to wire the two existing commands; M to give
  it a Snippets-style editable list.
- **Appcast items ship with no release notes** — the `<item>` template in
  `scripts/publish-sparkle.sh:483-497` emits no `<description>` or
  `sparkle:releaseNotesLink`, so anyone who accepts an auto-update prompt sees
  a bare version bump with no explanation. S; matters now that the channel is
  live and reaching non-technical installs.
- **Onboarding covers OS permissions only** —
  `Sources/Voice/UI/OnboardingView.swift:15-46` is exactly three steps (Input
  Monitoring, Microphone, Accessibility) and stops. Nothing teaches the
  push-to-talk gesture or explains local-vs-cloud engines, which is precisely
  what a peer install now needs. S-M.
- **No crash/error visibility for installs the maintainer can't see** — no
  MetricKit, no crash reporting, nothing (`grep` finds no matches across
  `Sources/`). The only diagnostic is `VOICE_DEBUG=1` writing a local log,
  unusable by a non-technical user. M, and it needs a privacy decision first:
  the README explicitly promises transcript text is never logged, so any new
  outbound path must be scoped deliberately.
- **Core dictation pipeline has zero test coverage** —
  `ActivationController`, `AudioRecorder`, `TextInjector`,
  `StreamingCoordinator`, and `ASREngineSelector`'s orchestration have no
  tests, while being the highest-churn files in the repo (18/16/11 commits
  since 2026-06-01). L effort — needs protocol seams for the audio and
  Accessibility APIs before unit tests are possible. Start with
  `ASREngineSelector`'s pure decision logic.
- **`SparkleSupport` launch gate is untested** (`SparkleSupport.swift:9-47`) —
  `hasRealFeedURL` / `hasNonEmptyPublicEDKey` are pure and trivially testable;
  the cert-OID check needs a seam. S. The existing
  `scripts/test-publish-sparkle-helpers.sh` covers the *publish script*, not
  this runtime gate.
- **Store persistence untested outside Dictionary** — folded into plan 018's
  test plan.
- **`SettingsView.swift:21-248`** — a single 227-line `var body`. Mechanical
  extraction into named sections; no logic change. S-M.
- **`CloudModel.default = .assemblyAIUniversal3`** (`LocalModel.swift:92`) —
  the default constant names a *retired* engine. Not a live bug
  (`selectableOrFallback` sanitizes it at read time), but it's a trap for the
  next reader. Noted in plan 019's maintenance notes.
- **No git tags** — the repo has a full signed-DMG + appcast release pipeline
  and has shipped 0.1.1 and 0.1.2, but `git tag -l` is empty, so no commit is
  identifiable as a released build. Tagging on publish is a one-line addition
  to the release flow.

### Findings considered and rejected this round

- **Sparkle EdDSA enforcement** — checked and clean. `updaterDelegate` is nil
  (`AppDelegate.swift:57`), so Sparkle's default signature verification and
  version comparison are untouched; no `http://` fallback exists; the publish
  script fails closed on a missing/mismatched public key and on non-increasing
  versions. Not a finding.
- **Repo secret hygiene** — `.gitignore` covers the EdDSA key patterns and
  `Config/Team.xcconfig`; `git ls-files` turns up no key- or credential-shaped
  tracked files. Clean.
- **Tests touching production stores** — explicitly re-checked, since a test
  suite once destroyed the operator's live API key. All 11 suites are properly
  namespaced (`KeychainAccountConsistencyTests` uses a `.TESTS` service;
  `DictionaryLearnTests` uses a temp directory). No violation.
- **`ModelManager.delete()` racing its own cancelled download**
  (`ModelManager.swift:313-321`) — plausible but LOW confidence: it depends on
  cancellation behavior inside WhisperKit/HubApi, which is outside this repo.
  One-line fix (`await` the cancelled task before removing the folder) if it
  ever manifests as a stuck download state; not worth a plan on speculation.
- **Vestigial `#available(macOS 26.0, *)` wrappers** (4 sites: 
  `TransformRunner.swift:28`, `CleanupService.swift:278`,
  `UI/TransformsView.swift:52`, `UI/SettingsStore.swift:409`) — the false
  branch is unreachable at a 26.0 floor, but the compiler may still require
  the annotation for the `FoundationModels` API surface. Compile-and-see change
  with near-zero payoff; recorded in plan 020's maintenance notes instead.
  (A subagent also cited `CleanupService.swift:100` — verified, no such branch
  there.)
- **`StreamingCoordinator` session pointers racing the audio tap** — real, same
  class as the plan-017 race, but deliberately deferred: plan 016 is editing
  that file. Noted as the follow-up in 017's maintenance notes.

## Audited but not planned (2026-07-09 round — ask to promote)

- Finding #8: response validation/decode copy-pasted 7-9× across providers
  (`CloudTranscriber.swift`, `CleanupService.swift`) — M effort, HIGH value
  for future provider work.
- Finding #9: extract the pure stitch/dedupe logic (lines 626-1108) from the
  WebSocket transport god-file — do AFTER 002.
- Finding #10: `HistoryStore.save()` full-file rewrite on the main thread per
  dictation — debounce like `ScratchpadStore`.
- Finding #13: fixture tests for the 5 cloud-provider response parsers;
  characterization tests for `CleanupService` (highest-churn file).

## Findings considered and rejected

- **"Prompt-injection" comment in `parakeet_transcribe.py:2`** — verified as
  residue of the workspace's own agent-routing tooling (commit `8ef794f`),
  not hostile; removal folded into plan 004 Step 5.
- **`StreamingTranscriber.swift` as unquarantined dead code** — the gate
  comment at `ASREngineSelector.swift:186-190` documents keeping it for a
  live-preview revisit; that satisfies the Phase-0 "quarantine" intent.
  Optional deletion only; not worth a plan.
- **AudioRecorder actor migration** — still NSLock-by-discipline despite the
  overhaul plan's Phase-4 claim, but L-effort/HIGH-risk on the most
  timing-sensitive path; parked until characterization tests exist.
- **TransformRunner lacking CleanupPrompt's anti-injection guards** —
  plausibly intentional (transforms are user-authored prompts meant to act on
  the text); needs a product decision, not a fix plan.
- **SwiftLint/formatter** — low value for a single-user, agent-written repo;
  revisit only if style drift becomes a review burden.
- **Cleanup-provider parity (Groq et al.)** — recorded as a cheap follow-up
  (Groq serves an OpenAI-compatible endpoint); not selected this round.
